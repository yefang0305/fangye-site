# 网站更新指南

`fangye.cc` 是一个 Astro 静态站，托管在 Cloudflare Pages，绑定 GitHub 仓库自动部署。

## 整体流程

```
本地改内容  →  git push 到 GitHub  →  Cloudflare 自动构建并发布到 fangye.cc
```

每次 push 后，Cloudflare 会自动重新构建，约 1-2 分钟后线上更新。

## 一、写新文章

文章是自动收录的，**只要在 `src/content/articles/` 新建一个 `.md` 文件即可**，不用改任何别的地方。

1. 新建 `src/content/articles/英文-slug.md`，开头写好 frontmatter：

   ```markdown
   ---
   title: "文章标题"
   description: "一句话简介，会显示在列表和文章顶部"
   pubDate: 2026-06-01
   tags: ["标签1", "标签2"]
   featured: false
   ---

   正文从这里开始……
   ```

2. 字段说明：
   - `pubDate`：发布日期，列表按它从新到旧排序
   - `tags`：标签数组
   - `featured`：设为 `true` 会被标为精选

3. 配图：图片放到 `public/images/你的-slug/图.png`，正文里用根路径引用：

   ```markdown
   ![图片说明](/images/你的-slug/图.png)
   ```

## 二、加工具

工具列表在 `src/data/tools.ts`，是一个数组。

- **只想加一张展示卡片**（不带可交互页面）：在数组里加一条，`status: 'showcase'`、`hasPage: false`。
- **要做能在站内直接用的工具**：
  1. 复制 `src/pages/tools/` 里现有的一个 `.astro` 文件，改成你的新工具页面。
  2. 在 `tools.ts` 加一条，`status: 'usable'`、`hasPage: true`、`href: '/tools/你的-slug/'`。

## 三、本地预览与上线

```bash
# 本地预览（改完实时看效果）
npm run dev

# 确认无误后上线
git add -A
git commit -m "新增文章：xxx"
git push
```

push 完成后，到 Cloudflare Pages 项目页可以看到构建进度，构建成功后 fangye.cc 自动更新。

## 常用命令

| 命令 | 作用 |
|------|------|
| `npm run dev` | 启动本地预览服务器 |
| `npm run build` | 本地构建，检查有没有报错 |
| `npm run preview` | 预览构建后的产物 |
