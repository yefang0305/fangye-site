Status
DONE

Role
implementer

Summary
Created a standalone local-ASR project under `J:\MagicTool\个人网站\tools\抖音文案提取` with 6 files: CLI entrypoint (`cli.py`), ASR engine (`asr_engine.py`), text cleaning rules (`rule_cleaner.py`), config/argparse/output module (`config.py`), non-inference tests (`test_local.py`), dependency file (`requirements.txt`), plus `README.md` and `.gitignore`. All logic is self-contained — zero runtime imports from the source project. `python -m compileall .` passes cleanly, all 17 non-inference tests pass, and `git status --short` on the source project confirms no files were modified by this task.

Changed Files
- J:\MagicTool\个人网站\tools\抖音文案提取\asr_engine.py
- J:\MagicTool\个人网站\tools\抖音文案提取\rule_cleaner.py
- J:\MagicTool\个人网站\tools\抖音文案提取\config.py
- J:\MagicTool\个人网站\tools\抖音文案提取\cli.py
- J:\MagicTool\个人网站\tools\抖音文案提取\requirements.txt
- J:\MagicTool\个人网站\tools\抖音文案提取\test_local.py
- J:\MagicTool\个人网站\tools\抖音文案提取\README.md
- J:\MagicTool\个人网站\tools\抖音文案提取\.gitignore

Verification
- `python -m compileall .` from `J:\MagicTool\个人网站\tools\抖音文案提取` — compiled all 5 `.py` files successfully, no syntax errors.
- `python test_local.py` from `J:\MagicTool\个人网站\tools\抖音文案提取` — 17 passed, 0 failed (covers argument parsing, video discovery, output writing, text cleaning rules).
- `git status --short` from `J:\MagicTool\Standalone\VideoCut\video_tool` — shows only pre-existing changes; no files in `J:\MagicTool\Standalone\VideoCut\video_tool` were created, modified, or deleted by this task.
- Grep for `Standalone|VideoCut|video_tool` under target directory — no matches, confirming zero runtime import dependency on the source project.

Findings
- The source project's `local_asr_engine.py` depends on `core.config` and `core.config.get_app_dir()`; these were replaced with a standalone `config.py` using `argparse` and environment variables.
- The source project's `rule_cleaner.py` had no external dependencies, so it was adapted directly with only a `from __future__ import annotations` addition.
- No non-inference test framework dependency (pytest not used) — `test_local.py` is self-executing plain Python with `tempfile` for isolated file discovery/output tests.

Final Result
DONE

Risks Or Follow-ups
- The tool requires `faster-whisper` and FFmpeg at runtime; these cannot be tested in this environment (no GPU, no FFmpeg in PATH). The acceptance criteria explicitly allow skipping inference tests.
- The `--no-clean` flag bypasses text cleaning; the cleaning rules (`ASR_CORRECTIONS`, filler removal, sales detection) are domain-specific to Chinese 口播 content and may need tuning for different content verticals.
- Model download is lazy (triggered on first `transcribe()` call); if the target machine has no internet or restricted HF access, the user must pre-download models manually to `--model-dir`.