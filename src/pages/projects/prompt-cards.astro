---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectPageLayout from '../../components/ProjectPageLayout.astro';
---

<BaseLayout title="提示词卡片库" description="把常用提示词整理成可复用的任务卡片，让 AI 协作更一致。">
  <ProjectPageLayout
    title="提示词卡片库"
    description="把日常反复使用的提示词整理成结构化卡片，让每次 AI 协作都有一致的起点和可预期的输出。"
    eyebrow="Project"
    techStack={[
      { label: '前端', items: ['Markdown', 'YAML 元数据', '静态站点生成'] },
      { label: '工具链', items: ['VS Code', 'Git', 'Any LLM Client'] },
    ]}
  >
    <h2>项目定位</h2>
    <p>
      提示词卡片库是一个<strong>提示词资产管理系统</strong>。不是又一个"提示词大全"，而是把真正用过的、验证过的提示词拆成任务卡片——
      每张卡片包含场景说明、输入格式、期望输出和已知边界，方便在不同项目和不同模型之间复用。
    </p>

    <h2>核心能力</h2>
    <div class="capability-grid">
      <div class="capability-card">
        <svg class="capability-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="3" y1="9" x2="21" y2="9"/><line x1="9" y1="21" x2="9" y2="9"/></svg>
        <h4>任务卡片格式</h4>
        <p>统一的 Markdown + YAML 格式，每张卡片定义角色、输入、输出和约束。</p>
      </div>
      <div class="capability-card">
        <svg class="capability-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        <h4>按场景检索</h4>
        <p>按任务类型、领域和模型版本分类，快速找到当前场景适用的卡片。</p>
      </div>
      <div class="capability-card">
        <svg class="capability-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.83 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/><path d="m15 5 4 4"/></svg>
        <h4>持续迭代</h4>
        <p>每次使用后记录效果和调整，让卡片库随实践不断进化。</p>
      </div>
    </div>

    <h2>使用场景</h2>
    <ul>
      <li>团队协作时共享同一套提示词规范，避免每个人"自由发挥"导致输出不一致。</li>
      <li>多模型切换时（GPT / Claude / Gemini），用卡片中标注的边界快速判断哪个模型更合适。</li>
      <li>新项目启动时直接引用已有卡片库，不需要从零构思提示词。</li>
    </ul>

    <div class="project-svg">
      <svg viewBox="0 0 720 260" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <linearGradient id="pc-grad" x1="0" y1="0" x2="1" y2="1">
            <stop offset="0%" stop-color="#142128"/>
            <stop offset="100%" stop-color="#0d161b"/>
          </linearGradient>
        </defs>
        <!-- Cards -->
        <rect x="20" y="30" width="200" height="90" rx="6" fill="url(#pc-grad)" stroke="#2a4a3f" stroke-width="1.5"/>
        <text x="40" y="58" fill="#9ff5d7" font-size="13" font-weight="bold" font-family="monospace">Role: Code Reviewer</text>
        <text x="40" y="80" fill="#a9bbb6" font-size="11" font-family="monospace">Input: code diff + context</text>
        <text x="40" y="100" fill="#a9bbb6" font-size="11" font-family="monospace">Output: review checklist</text>

        <rect x="260" y="30" width="200" height="90" rx="6" fill="url(#pc-grad)" stroke="#2a4a3f" stroke-width="1.5"/>
        <text x="280" y="58" fill="#9ff5d7" font-size="13" font-weight="bold" font-family="monospace">Role: Text Cleaner</text>
        <text x="280" y="80" fill="#a9bbb6" font-size="11" font-family="monospace">Input: raw transcript</text>
        <text x="280" y="100" fill="#a9bbb6" font-size="11" font-family="monospace">Output: polished article</text>

        <rect x="500" y="30" width="200" height="90" rx="6" fill="url(#pc-grad)" stroke="#2a4a3f" stroke-width="1.5"/>
        <text x="520" y="58" fill="#9ff5d7" font-size="13" font-weight="bold" font-family="monospace">Role: Translator</text>
        <text x="520" y="80" fill="#a9bbb6" font-size="11" font-family="monospace">Input: zh-CN text</text>
        <text x="520" y="100" fill="#a9bbb6" font-size="11" font-family="monospace">Output: en-US + glossary</text>

        <!-- Flow arrows -->
        <line x1="220" y1="75" x2="255" y2="75" stroke="#47d6b2" stroke-width="1.5" marker-end="url(#arrow)"/>
        <line x1="460" y1="75" x2="495" y2="75" stroke="#47d6b2" stroke-width="1.5" marker-end="url(#arrow)"/>
        <defs><marker id="arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto"><path d="M0,0 L10,5 L0,10 Z" fill="#47d6b2"/></marker></defs>

        <!-- Storage -->
        <rect x="250" y="155" width="220" height="40" rx="6" fill="#0d161b" stroke="#2a4a3f" stroke-width="1" stroke-dasharray="4 3"/>
        <text x="305" y="180" fill="#78908c" font-size="12" font-family="monospace">~/.prompt-cards/</text>

        <!-- Client logos -->
        <text x="60" y="200" fill="#78908c" font-size="10" font-family="monospace">GPT</text>
        <text x="240" y="200" fill="#78908c" font-size="10" font-family="monospace">Claude</text>
        <text x="420" y="200" fill="#78908c" font-size="10" font-family="monospace">Gemini</text>
        <text x="590" y="200" fill="#78908c" font-size="10" font-family="monospace">Local LLM</text>

        <!-- Lines from storage to clients -->
        <line x1="320" y1="195" x2="100" y2="198" stroke="#2a4a3f" stroke-width="0.8"/>
        <line x1="340" y1="195" x2="270" y2="198" stroke="#2a4a3f" stroke-width="0.8"/>
        <line x1="380" y1="195" x2="440" y2="198" stroke="#2a4a3f" stroke-width="0.8"/>
        <line x1="400" y1="195" x2="610" y2="198" stroke="#2a4a3f" stroke-width="0.8"/>
      </svg>
    </div>

    <h2>本地使用</h2>
    <p>卡片库以纯文本文件存储，不依赖任何特定工具。可以直接用 VS Code 打开编辑，也可以集成到任何支持自定义 system prompt 的 LLM 客户端中。</p>
    <p>推荐目录结构：</p>
    <ul>
      <li><code>cards/coding/</code> — 代码审查、重构、测试生成</li>
      <li><code>cards/writing/</code> — 文章润色、翻译、摘要</li>
      <li><code>cards/workflow/</code> — 项目规划、任务分解、复盘</li>
    </ul>

    <div class="project-note">
      <p><strong>提示：</strong>卡片的效果取决于模型的上下文窗口和指令跟随能力。建议为不同模型版本维护对应的卡片变体。</p>
    </div>
  </ProjectPageLayout>
</BaseLayout>
